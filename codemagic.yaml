
workflows:
  android-xml-workflow:
    name: Android XML Build Workflow
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      vars:
        JAVA_VERSION: "17"
    scripts:
      - name: Set up local.properties
        script: |
          cd برنامج
          echo "sdk.dir=$ANDROID_SDK_ROOT" > local.properties
      - name: Build Android APK
        script: |
          cd برنامج
          chmod +x gradlew
          ./gradlew assembleRelease
    artifacts:
      - برنامج/app/build/outputs/apk/**/*.apk
